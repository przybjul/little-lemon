# Visual refresh + rotating lemon

- Introduced brand color variables in `src/theme.css` (F4CE14, 495E57, EE9972, FBDABB, EDEFEE, 333333).
- Imported the theme in `src/index.js`.
- Replaced the React atom with a **rotating lemon**: `src/logo.svg`.
- Kept the existing `.App-logo` spin in `src/App.css` (20s linear infinite).
- Updated CSS files to reference the variables where appropriate.

If you want to tweak the palette, edit `src/theme.css`.
